@model MVC_Plan.Models.DegreeRequirement

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DegreeRequirement</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="DegreeRequirementID" />
            <div class="form-group">
                <label asp-for="StudentTermID" class="control-label"></label>
                <select asp-for="StudentTermID" class="form-control" asp-items="ViewBag.StudentTermID"></select>
                <span asp-validation-for="StudentTermID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RequirementID" class="control-label"></label>
                <select asp-for="RequirementID" class="form-control" asp-items="ViewBag.RequirementID"></select>
                <span asp-validation-for="RequirementID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
